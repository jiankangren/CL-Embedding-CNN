# CL-Embedding-CNN
Character-level Embedding with Convolutional Neural Network for Malicious Web Request Detection

You should first run "Embedding_Learning.py" to generate character embeddings for raw datasets, then train the CNN model with these character embeddings by running "train_CNN.py" 

